CREATE DATABASE ImageAnnotate

CREATE TABLE class (classID INT AUTO_INCREMENT, name TEXT NOT NULL, PRIMARY KEY (classID));

INSERT INTO class (name) VALUES ("class1"),("class2"),("class3");

CREATE TABLE annotation (annID INTEGER AUTO_INCREMENT,imgName TEXT NOT NULL,classID INTEGER NOT NULL,xCoor FLOAT  NOT NULL,yCoor FLOAT  NOT NULL,leng FLOAT  NOT NULL,height FLOAT  NOT NULL,PRIMARY KEY (annID),FOREIGN KEY (classID) REFERENCES class(classID));

